# Changelog

## 0.1.1

- Updated `displayName` and `description`: emphasis on precise file references for AI prompts
- Expanded `keywords` (ai, llm, prompt-engineering, context, cursor, copilot, chatgpt, claude) for better Marketplace discoverability
- Added `Chat` category alongside `Other`
- README rewritten for the new positioning
- Project translated to English (code comments, package.json, README, changelog)

## 0.0.1

- First release: autocomplete of file and folder paths via `@` in markdown
- Settings `excludeGlobs`, `maxFiles`, `includeDirectories`, `maxSuggestions`, `trace`
- Command `mdPathMentions.rebuildIndex`
